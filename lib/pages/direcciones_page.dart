import 'package:flutter/cupertino.dart';

class DireccioensPage extends StatelessWidget {
  const DireccioensPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Text('direcciones page');
  }
}
